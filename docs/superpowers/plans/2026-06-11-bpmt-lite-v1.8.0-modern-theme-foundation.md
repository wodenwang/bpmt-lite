# bpmt-lite v1.8.0 桌面现代化主题基础实施计划

## 目标

完成 `v1.8.0` CSS-only 桌面现代化主题基础，并保留 Product Design 设计依据、运行验证、截图证据和回滚说明。

## Task 1: 版本与分支

- [x] 从 `main` 创建独立分支 `codex/bpmt-lite-v1.8.0-modern-theme`。
- [x] 确认当前发布基线为 `v1.7.5`。
- [x] 将 Maven、默认镜像 tag、安装脚本默认 ref 更新到 `1.8.0` / `v1.8.0`。

## Task 2: Product Design 设计收敛

- [x] 使用 Product Design `get-context` playback：产品、视觉来源和交互级别已由用户与现有规划确认。
- [x] 评估三个视觉方向：保守清理、中性工作台、品牌强化。
- [x] 选择中性工作台主题作为实现方向。
- [x] 写入 `docs/superpowers/specs/2026-06-11-bpmt-lite-v1.8.0-modern-theme-foundation-design.md`。

## Task 3: CSS-only 实现

- [x] 新增 `platform/src/main/webapp/css/bpmt-modern.css`。
- [x] 在 `platform/src/main/webapp/include/html_include.jsp` 最后引入。
- [x] 覆盖基础控件、jQuery UI、BPMT `ws-*` 控件、frame、登录页、zTree、chosen 和 AI prompt dialog。

## Task 4: 文档与发布面

- [x] 更新 `README.md`。
- [x] 更新 `AGENTS.md`。
- [x] 新增 `docs/release-v1.8.0.md`。
- [x] 新增 `docs/v2.0.0/desktop-ui-qa-checklist.md`。
- [x] 归档 `docs/v1.8.0/api-reference.md` 和 `docs/v1.8.0/openapi.json`。

## Task 5: 验证

- [x] 运行 Maven 编译。
- [x] 构建本地 Web/API 镜像或确认可运行路径。
- [x] 启动本地服务。
- [x] 使用浏览器验证 `/`、登录后首页、`manage.xhtml`、第三方系统列表和 AI 接入提示词弹窗。
- [x] 保存 1440x900 截图到 `docs/v2.0.0/screenshots/v1.8.0/`。
- [x] Product Design 自审截图并修复发现的问题。

## Task 6: 完成标准

- [x] 无 JavaScript console error。
- [x] 关键页面不出现文本重叠、按钮换行、弹窗溢出、表格破版。
- [x] CSS 可通过移除全局引入单点回滚。
- [x] `git diff` 中没有无关业务逻辑修改。

## 验证记录

- `mvn -s settings.local.xml -DskipTests compile`：通过，Reactor 全部 `SUCCESS`。
- `scripts/build-image.sh`：通过，生成并验证 `ghcr.io/wodenwang/bpmt-lite:1.8.0`。
- `scripts/build-api-image.sh`：通过，生成并验证 `ghcr.io/wodenwang/bpmt-lite-api:1.8.0`。
- `BPMT_HTTP_PORT=18080 docker compose up -d`：通过，`bpmt-web`、`bpmt-api`、`bpmt-nginx`、`bpmt-mariadb` 运行中。
- HTTP smoke：`/`、`/ueditor/`、`/api/docs/`、`/api/openapi.json`、`/css/bpmt-modern.css` 均返回 `200`。
- Playwright 1440x900 截图：`01-login.png`、`02-home.png`、`03-manage.png`、`04-thirdpart-list.png`、`05-ai-prompt-dialog.png`、`06-api-docs.png`。
- Product Design 自审：发现并修复 `ws-bar` 第一个按钮过度主按钮化的问题，复验无新增 console error。
