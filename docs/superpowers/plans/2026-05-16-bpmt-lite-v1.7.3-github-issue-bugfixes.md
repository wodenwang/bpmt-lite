# bpmt-lite v1.7.3 GitHub Issue Bugfixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `v1.7.3` 收敛为基于 `v1.7.2` 的补丁版本，修复当前 GitHub open issues `#18` 和 `#19`，不新增业务功能。

**Architecture:** 本版本分两条窄路径处理：OAuth 微信登录失败不再把 BPMT 本地登录态建立失败泛化为“微信登录失败”；OpenAPI 文档统一中文描述风格，保留必要的 `dyn`、`rep_list`、endpoint、schema 等技术标识。API 路径、认证协议、数据库结构、Docker 运行栈和既有 OAuth 主流程保持兼容。

**Tech Stack:** Java 8、Maven 3、Tomcat 7、JUnit 4、Jackson、Docker Compose、GHCR multi-arch images。

---

## 当前 Issue 基线

截至 2026-05-16 当前打开的 GitHub issue：

- `#18`：[优化 OAuth 微信登录失败页，展示更清晰的失败原因](https://github.com/wodenwang/bpmt-lite/issues/18)
- `#19`：[/api/openapi.json 文档的前后风格不一致](https://github.com/wodenwang/bpmt-lite/issues/19)

已关闭且不纳入本版本重复处理的近期 issue：

- `#17`：已在 `v1.7.2` 修复报表视图创建 500。
- `#15`：已在 `v1.7.2` 修复动态表视图写入后的运行时缓存失效。
- `#14`：已在 `v1.7.2` 修复控制面板版本显示。

## 版本边界

- 基线版本：`v1.7.2`
- 目标版本：`v1.7.3`
- 类型：patch release
- 不新增 API endpoint
- 不改变 `/oauth/authorize`、`/oauth/token`、`/oauth/userinfo` 协议语义
- 不改变 `/api/v1/*` HMAC 签名规则
- 不新增数据库表，不修改默认初始化 SQL
- 不改变 `web`、`api`、`nginx`、`mariadb` compose 服务拓扑
- Docker/GHCR 发布仍需 Web/API `linux/amd64` 和 `linux/arm64` multi-arch manifest

## File Structure

- Create: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginException.java`
  - 承载微信授权成功后 BPMT 本地登录态建立失败的分类结果，包括 `reason`、安全展示文案和可选 `userId`。
- Create: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginFailureClassifier.java`
  - 将 `SessionManager.doUserLogin()` 常见异常文案分类为可行动的 OAuth 微信登录错误原因。
- Modify: `platform/src/main/java/com/riversoft/module/oauth/wechat/RealWechatOAuthProvider.java`
  - 企业微信 `QyUser.userId` 已取得但 `SessionManager.doUserLogin()` 失败时，抛出带 `userId` 的 `OAuthWechatLoginException`。
- Modify: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginService.java`
  - 捕获 `OAuthWechatLoginException`，返回分类后的 `reason/message`，日志保留 `requestId` 上游可关联信息、`clientId`、`thirdpartKey`、`wechatType`、`wechatKey`、`userId` 和失败分类。
- Modify: `platform/src/main/java/com/riversoft/module/oauth/OAuthAction.java`
  - 对 `bpmt_*` 类失败展示 `OAuth BPMT 登录失败`，避免页面继续暗示微信侧失败。
- Modify: `platform/src/test/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginServiceTest.java`
  - 覆盖平台暂停、用户不存在、用户停用、IP 限制、组织角色异常和未知 BPMT 登录失败分类。
- Modify: `platform/src/test/java/com/riversoft/module/oauth/OAuthActionTest.java`
  - 覆盖 `bpmt_*` 失败使用 BPMT 登录失败标题，配置类失败仍使用 OAuth 微信登录失败标题。
- Modify: `api/src/main/webapp/openapi.json`
  - `info.version`、`info.description` 和报表视图相关 summary/description 统一中文风格。
- Modify: `api/src/main/webapp/docs/index.html`
  - API 文档首页版本和报表视图说明同步 `v1.7.3`。
- Modify: `api/src/test/java/com/riversoft/api/http/ApiDocsContractTest.java`
  - 增加 OpenAPI 描述风格测试，防止重新出现中英文混杂的说明句。
- Create: `docs/v1.7.3/api-reference.md`
  - 归档 `v1.7.3` API 文档说明，强调本版本是文档风格修复而非接口能力扩展。
- Create: `docs/v1.7.3/openapi.json`
  - 从 `api/src/main/webapp/openapi.json` 复制 `v1.7.3` OpenAPI 快照。
- Create: `docs/release-v1.7.3.md`
  - 记录修复项、验收命令、Docker/GHCR 发布结果和 issue 关闭情况。
- Modify: `pom.xml`
  - Maven 版本切到 `1.7.3`。
- Modify: `README.md`
  - 当前版本、默认镜像 tag、版本历史和文档入口更新到 `v1.7.3`。
- Modify: `AGENTS.md`
  - 记录 `v1.7.3` 是基于 `v1.7.2` 修复 OAuth 微信登录失败页和 OpenAPI 文档风格的补丁版本，并补充本计划为 source of truth。

---

### Task 1: 建立 v1.7.3 工作基线

**Files:**
- Read: `AGENTS.md`
- Read: `docs/release-v1.7.2.md`
- Read: `docs/superpowers/plans/2026-05-16-bpmt-lite-v1.7.2-github-issue-bugfixes.md`

- [ ] **Step 1: 确认本地和远端同步**

Run:

```bash
git fetch origin --tags --prune
git status --short --branch
git log --oneline --decorate -5
gh issue list --repo wodenwang/bpmt-lite --state open --limit 50
```

Expected:

```text
## main...origin/main
```

Open issues include only `#18` and `#19` for this release scope.

- [ ] **Step 2: 建立工作分支**

Run:

```bash
git switch -c codex/bpmt-lite-v1.7.3-github-issues
```

Expected:

```text
Switched to a new branch 'codex/bpmt-lite-v1.7.3-github-issues'
```

- [ ] **Step 3: 记录 issue 正文作为执行依据**

Run:

```bash
mkdir -p docs/v1.7.3
gh issue view 18 --repo wodenwang/bpmt-lite --json number,title,body,url > /tmp/bpmt-lite-issue-18.json
gh issue view 19 --repo wodenwang/bpmt-lite --json number,title,body,url > /tmp/bpmt-lite-issue-19.json
```

Expected: two JSON files exist under `/tmp/` and include the current issue title/body.

---

### Task 2: Fix #18 OAuth 微信登录失败分类

**Files:**
- Create: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginException.java`
- Create: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginFailureClassifier.java`
- Modify: `platform/src/main/java/com/riversoft/module/oauth/wechat/RealWechatOAuthProvider.java`
- Modify: `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginService.java`
- Modify: `platform/src/test/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginServiceTest.java`

- [ ] **Step 1: Write failing classifier tests**

In `platform/src/test/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginServiceTest.java`, add tests before `fakeProviderAppendsDefaultCodeToCallbackWithExistingQuery()`:

```java
@Test
public void classifiesBpmtPauseDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("系统维护中,暂停用户登陆."));

    assertEquals("bpmt_login_paused", failure.getReason());
    assertEquals("woden", failure.getUserId());
    assertEquals("微信授权已成功，但 BPMT 当前处于维护/暂停模式，用户[woden]无法建立登录态。请管理员检查 safe.role 或 safe.admin 配置。", failure.getSafeMessage());
}

@Test
public void classifiesBpmtUserNotFoundDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("找不到用户[woden]."));

    assertEquals("bpmt_user_not_found", failure.getReason());
    assertEquals("微信授权已成功，但 BPMT 中找不到用户[woden]。请管理员检查企业微信 UserId 与 BPMT 用户账号映射。", failure.getSafeMessage());
}

@Test
public void classifiesBpmtUserDisabledDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("用户[woden]账号已失效."));

    assertEquals("bpmt_user_disabled", failure.getReason());
    assertEquals("微信授权已成功，但 BPMT 用户[woden]账号已失效。请管理员启用用户或更换绑定账号。", failure.getSafeMessage());
}

@Test
public void classifiesBpmtIpDeniedDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("用户[woden]当前网络环境不安全,请更换网络环境登陆."));

    assertEquals("bpmt_user_ip_denied", failure.getReason());
    assertEquals("微信授权已成功，但 BPMT 拒绝了用户[woden]当前网络环境。请管理员检查用户 IP 白名单或上游代理地址。", failure.getSafeMessage());
}

@Test
public void classifiesBpmtRelationshipInvalidDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("无法找到用户[woden]归属的组织与角色,无法登陆系统."));

    assertEquals("bpmt_user_relationship_invalid", failure.getReason());
    assertEquals("微信授权已成功，但 BPMT 用户[woden]的组织或角色关系不可用。请管理员检查用户所属组织、角色和权限组。", failure.getSafeMessage());
}

@Test
public void classifiesUnknownBpmtLoginFailureDuringWechatLogin() {
    OAuthWechatLoginException failure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("unexpected local login failure"));

    assertEquals("bpmt_login_failed", failure.getReason());
    assertEquals("微信授权已成功，但 BPMT 本地登录态建立失败。请联系管理员并提供 Request ID。", failure.getSafeMessage());
}
```

- [ ] **Step 2: Run tests and confirm compilation failure**

Run:

```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-1.8.jdk/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"
mvn -s settings.local.xml -pl platform -Dtest=OAuthWechatLoginServiceTest test
```

Expected before implementation:

```text
cannot find symbol
  symbol:   variable OAuthWechatLoginFailureClassifier
```

- [ ] **Step 3: Add `OAuthWechatLoginException`**

Create `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginException.java`:

```java
package com.riversoft.module.oauth.wechat;

public class OAuthWechatLoginException extends RuntimeException {
    private final String reason;
    private final String safeMessage;
    private final String userId;

    public OAuthWechatLoginException(String reason, String safeMessage, String userId, Throwable cause) {
        super(safeMessage, cause);
        this.reason = reason;
        this.safeMessage = safeMessage;
        this.userId = userId;
    }

    public String getReason() {
        return reason;
    }

    public String getSafeMessage() {
        return safeMessage;
    }

    public String getUserId() {
        return userId;
    }
}
```

- [ ] **Step 4: Add `OAuthWechatLoginFailureClassifier`**

Create `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginFailureClassifier.java`:

```java
package com.riversoft.module.oauth.wechat;

import org.apache.commons.lang3.StringUtils;

final class OAuthWechatLoginFailureClassifier {
    private OAuthWechatLoginFailureClassifier() {
    }

    static OAuthWechatLoginException classify(String userId, RuntimeException cause) {
        String uid = StringUtils.trimToEmpty(userId);
        String message = cause == null ? "" : StringUtils.trimToEmpty(cause.getMessage());
        if (message.contains("系统维护中,暂停用户登陆")) {
            return failure("bpmt_login_paused", uid, "微信授权已成功，但 BPMT 当前处于维护/暂停模式，用户[" + uid
                    + "]无法建立登录态。请管理员检查 safe.role 或 safe.admin 配置。", cause);
        }
        if (message.contains("找不到用户[")) {
            return failure("bpmt_user_not_found", uid, "微信授权已成功，但 BPMT 中找不到用户[" + uid
                    + "]。请管理员检查企业微信 UserId 与 BPMT 用户账号映射。", cause);
        }
        if (message.contains("账号已失效")) {
            return failure("bpmt_user_disabled", uid, "微信授权已成功，但 BPMT 用户[" + uid
                    + "]账号已失效。请管理员启用用户或更换绑定账号。", cause);
        }
        if (message.contains("当前网络环境不安全")) {
            return failure("bpmt_user_ip_denied", uid, "微信授权已成功，但 BPMT 拒绝了用户[" + uid
                    + "]当前网络环境。请管理员检查用户 IP 白名单或上游代理地址。", cause);
        }
        if (message.contains("归属的组织") || message.contains("归属的角色") || message.contains("组织与角色")) {
            return failure("bpmt_user_relationship_invalid", uid, "微信授权已成功，但 BPMT 用户[" + uid
                    + "]的组织或角色关系不可用。请管理员检查用户所属组织、角色和权限组。", cause);
        }
        return failure("bpmt_login_failed", uid, "微信授权已成功，但 BPMT 本地登录态建立失败。请联系管理员并提供 Request ID。", cause);
    }

    private static OAuthWechatLoginException failure(String reason, String userId, String safeMessage,
            RuntimeException cause) {
        return new OAuthWechatLoginException(reason, safeMessage, userId, cause);
    }
}
```

- [ ] **Step 5: Wrap enterprise-WeChat local login failures with `userId`**

In `platform/src/main/java/com/riversoft/module/oauth/wechat/RealWechatOAuthProvider.java`, replace the enterprise-WeChat login block:

```java
QyUser qyUser = QyOAuth2s.with(agentConfig.toCorpSetting()).userInfo(code);
SessionManager.doUserLogin(request, qyUser.getUserId());
return qyUser.getUserId();
```

with:

```java
QyUser qyUser = QyOAuth2s.with(agentConfig.toCorpSetting()).userInfo(code);
String userId = qyUser.getUserId();
try {
    SessionManager.doUserLogin(request, userId);
} catch (RuntimeException e) {
    throw OAuthWechatLoginFailureClassifier.classify(userId, e);
}
return userId;
```

- [ ] **Step 6: Preserve classified failure in the service response**

In `platform/src/main/java/com/riversoft/module/oauth/wechat/OAuthWechatLoginService.java`, add a catch block before the existing `catch (RuntimeException e)` in the code-login section:

```java
} catch (OAuthWechatLoginException e) {
    return error(thirdpart, wechatType, wechatKey, e.getUserId(), e.getReason(), e.getSafeMessage());
} catch (RuntimeException e) {
    return error(thirdpart, wechatType, wechatKey, null, "wechat_login_failed", "微信登录失败.");
}
```

- [ ] **Step 7: Add service-level classified failure test**

In `OAuthWechatLoginServiceTest`, add:

```java
@Test
public void returnsClassifiedBpmtLoginFailureWhenProviderReportsLocalLoginFailure() {
    TestWechatOAuthProvider provider = new TestWechatOAuthProvider();
    provider.loginFailure = OAuthWechatLoginFailureClassifier.classify("woden",
            new RuntimeException("系统维护中,暂停用户登陆."));
    OAuthWechatLoginService service = new OAuthWechatLoginService(provider);
    MockHttpServletRequest request = wechatRequest();
    request.setParameter("code", "secret-code");

    OAuthWechatLoginResult result = service.handle(request, new MockHttpServletResponse(), agentThirdpart());

    assertEquals(OAuthWechatLoginStatus.ERROR, result.getStatus());
    assertEquals("bpmt_login_paused", result.getReason());
    assertEquals("微信授权已成功，但 BPMT 当前处于维护/暂停模式，用户[woden]无法建立登录态。请管理员检查 safe.role 或 safe.admin 配置。", result.getMessage());
    assertEquals(1, provider.loginCalls);
}
```

- [ ] **Step 8: Run focused OAuth WeChat tests**

Run:

```bash
mvn -s settings.local.xml -pl platform -Dtest=OAuthWechatLoginServiceTest test
```

Expected:

```text
BUILD SUCCESS
```

---

### Task 3: Fix #18 OAuth 错误页标题与提示

**Files:**
- Modify: `platform/src/main/java/com/riversoft/module/oauth/OAuthAction.java`
- Modify: `platform/src/test/java/com/riversoft/module/oauth/OAuthActionTest.java`

- [ ] **Step 1: Write failing action title test**

In `platform/src/test/java/com/riversoft/module/oauth/OAuthActionTest.java`, add after `authorizeWechatConfigErrorShowsOauthErrorPage()`:

```java
@Test
public void authorizeWechatBpmtLoginErrorShowsBpmtErrorPageTitle() {
    TestOAuthAction action = new TestOAuthAction();
    action.loggedIn = false;
    action.wechatResult = OAuthWechatLoginResult.error("bpmt_login_paused",
            "微信授权已成功，但 BPMT 当前处于维护/暂停模式，用户[woden]无法建立登录态。请管理员检查 safe.role 或 safe.admin 配置。");
    MockHttpServletRequest request = authorizeRequest();
    MockHttpServletResponse response = new MockHttpServletResponse();

    action.authorize(request, response);

    assertEquals("/xhtml/oauth/error.jsp", action.forwardedPage);
    assertEquals("OAuth BPMT 登录失败", request.getAttribute("oauthErrorTitle"));
    assertEquals("微信授权已成功，但 BPMT 当前处于维护/暂停模式，用户[woden]无法建立登录态。请管理员检查 safe.role 或 safe.admin 配置。",
            request.getAttribute("oauthErrorMessage"));
    assertEquals("request-a", request.getAttribute("requestId"));
    assertNull(action.loginTarget);
    assertNull(response.getRedirectedUrl());
}
```

- [ ] **Step 2: Run focused action test and confirm failure**

Run:

```bash
mvn -s settings.local.xml -pl platform -Dtest=OAuthActionTest#authorizeWechatBpmtLoginErrorShowsBpmtErrorPageTitle test
```

Expected before implementation:

```text
expected:<OAuth BPMT 登录失败> but was:<OAuth 微信登录失败>
```

- [ ] **Step 3: Implement title selection**

In `platform/src/main/java/com/riversoft/module/oauth/OAuthAction.java`, replace:

```java
showBrowserError(request, response, "OAuth 微信登录失败", wechat.getMessage(), requestId);
```

with:

```java
showBrowserError(request, response, wechatErrorTitle(wechat), wechat.getMessage(), requestId);
```

Add this helper near `showBrowserError(...)`:

```java
private String wechatErrorTitle(OAuthWechatLoginResult wechat) {
    String reason = wechat == null ? null : wechat.getReason();
    if (StringUtils.startsWith(reason, "bpmt_")) {
        return "OAuth BPMT 登录失败";
    }
    return "OAuth 微信登录失败";
}
```

- [ ] **Step 4: Run OAuth action tests**

Run:

```bash
mvn -s settings.local.xml -pl platform -Dtest=OAuthActionTest test
```

Expected:

```text
BUILD SUCCESS
```

---

### Task 4: Fix #19 OpenAPI 文档风格

**Files:**
- Modify: `api/src/main/webapp/openapi.json`
- Modify: `api/src/main/webapp/docs/index.html`
- Modify: `api/src/test/java/com/riversoft/api/http/ApiDocsContractTest.java`
- Create: `docs/v1.7.3/openapi.json`
- Create: `docs/v1.7.3/api-reference.md`

- [ ] **Step 1: Write failing OpenAPI description style test**

In `api/src/test/java/com/riversoft/api/http/ApiDocsContractTest.java`, add after `openApiJsonPublishesReportViewRouteMatrix()`:

```java
@Test
public void openApiInfoDescriptionUsesConsistentChineseStyle() throws Exception {
    JsonNode root = new ObjectMapper().readTree(apiFile("src/main/webapp/openapi.json"));
    String description = root.path("info").path("description").asText();

    assertTrue(description.contains("BPMT Lite v1.7.3 API，提供动态表结构、dyn 动态表视图、数据库操作和报表视图配置接口。"));
    assertTrue(description.contains("报表视图接口挂载在 /api/v1/report-views"));
    assertTrue(description.contains("只管理 rep_list 报表视图对应的 VW_URL / VW_REPORT* 配置"));
    assertTrue(description.contains("HMAC 签名的 PATH 必须使用公开请求 URI"));
    assertFalse(description.contains("Includes dynamic table structure APIs"));
    assertFalse(description.contains("Report view APIs manage"));
    assertFalse(description.contains("button actions"));
}
```

Also update existing version assertions in `openApiJsonPublishesReportViewRouteMatrix()` and `docsIndexDescribesReportViewApi()` from `v1.7.1` to `v1.7.3`.

- [ ] **Step 2: Run docs test and confirm failure**

Run:

```bash
mvn -s settings.local.xml -pl api -Dtest=ApiDocsContractTest#openApiInfoDescriptionUsesConsistentChineseStyle test
```

Expected before implementation:

```text
java.lang.AssertionError
```

- [ ] **Step 3: Update OpenAPI `info`**

In `api/src/main/webapp/openapi.json`, set:

```json
"info": {
  "title": "BPMT Lite v1.7.3 API",
  "version": "1.7.3",
  "description": "BPMT Lite v1.7.3 API，提供动态表结构、dyn 动态表视图、数据库操作和报表视图配置接口。报表视图接口挂载在 /api/v1/report-views，只管理 rep_list 报表视图对应的 VW_URL / VW_REPORT* 配置，不执行 SQL、按钮动作或客户端脚本。HMAC 签名的 PATH 必须使用公开请求 URI，例如 /api/v1/report-views。"
}
```

- [ ] **Step 4: Normalize report-view operation summaries**

In `api/src/main/webapp/openapi.json`, update report-view operation summaries to Chinese while keeping endpoint and `rep_list` identifiers in descriptions:

```json
"summary": "分页列出报表视图"
```

```json
"summary": "创建报表视图"
```

```json
"summary": "校验报表视图快照"
```

```json
"summary": "导出报表视图快照"
```

```json
"summary": "整体替换报表视图快照"
```

```json
"summary": "删除报表视图配置"
```

```json
"summary": "替换报表视图单个配置区块"
```

Keep descriptions explicit that only `rep_list` is supported and SQL/scripts are not executed.

- [ ] **Step 5: Update API docs index**

In `api/src/main/webapp/docs/index.html`, update the visible version and report-view introductory copy to `v1.7.3`, and keep these strings present:

```html
v1.7.3
报表视图 API
/api/v1/report-views
/api/v1/report-views:validate
API 不执行 SQL
```

- [ ] **Step 6: Create versioned OpenAPI snapshot**

Run:

```bash
mkdir -p docs/v1.7.3
cp api/src/main/webapp/openapi.json docs/v1.7.3/openapi.json
python3 -m json.tool docs/v1.7.3/openapi.json >/tmp/bpmt-v173-openapi.json
cmp -s api/src/main/webapp/openapi.json docs/v1.7.3/openapi.json
```

Expected: commands exit with status `0`.

- [ ] **Step 7: Create v1.7.3 API reference**

Create `docs/v1.7.3/api-reference.md`:

```markdown
# bpmt-lite v1.7.3 API 参考

`v1.7.3` 基于 `v1.7.2` 修复 OpenAPI 文档风格问题，不新增 API endpoint，不改变 HMAC 签名、响应包装或权限边界。

## 本版本 API 变化

- `/api/openapi.json` 的 `info.description` 统一为中文说明，保留必要技术标识：`dyn`、`rep_list`、`VW_URL`、`VW_REPORT*`、`PATH`。
- `/api/openapi.json` 的报表视图接口 summary 统一为中文动词短语。
- `/api/docs/` 同步显示 `v1.7.3`。

## 接口范围

- 动态表结构 API：`/api/v1/dynamic-tables`
- 数据库操作 API：`/api/v1/database-operations/*`
- 动态表视图 API：`/api/v1/dynamic-table-views`
- 报表视图 API：`/api/v1/report-views`

报表视图 API 仍只管理 `rep_list` 报表视图配置，不执行 SQL、按钮动作或客户端脚本。

## OpenAPI 快照

当前版本 OpenAPI 快照见 [openapi.json](openapi.json)。
```

- [ ] **Step 8: Run API docs tests**

Run:

```bash
mvn -s settings.local.xml -pl api -Dtest=ApiDocsContractTest test
```

Expected:

```text
BUILD SUCCESS
```

---

### Task 5: Version, README, AGENTS, release notes

**Files:**
- Modify: `pom.xml`
- Modify: `README.md`
- Modify: `AGENTS.md`
- Create: `docs/release-v1.7.3.md`

- [ ] **Step 1: Bump Maven version to 1.7.3**

Run:

```bash
mvn -s settings.local.xml versions:set -DnewVersion=1.7.3 -DgenerateBackupPoms=false
```

Expected:

```text
BUILD SUCCESS
```

- [ ] **Step 2: Update README version facts**

In `README.md`, update:

```markdown
- 当前版本：`v1.7.3`
- 默认 Web 镜像：`ghcr.io/wodenwang/bpmt-lite:1.7.3`
- 默认 API 镜像：`ghcr.io/wodenwang/bpmt-lite-api:1.7.3`
```

Add a version history row immediately after `v1.7.2`:

```markdown
| `v1.7.3` | 2026-05-16 | 基于 `v1.7.2` 修复 OAuth 微信登录失败页提示和 `/api/openapi.json` 文档风格问题，不新增接口能力。 | [API](docs/v1.7.3/api-reference.md)、[OpenAPI](docs/v1.7.3/openapi.json) |
```

- [ ] **Step 3: Update AGENTS version rules**

In `AGENTS.md`, add after the `v1.7.2` bullet:

```markdown
- `v1.7.3` 是当前版本，基于 `v1.7.2` 修复 OAuth 微信登录失败页提示和 `/api/openapi.json` 文档风格问题。
```

Add a source-of-truth line after the `v1.7.2` source-of-truth line:

```markdown
- v1.7.3 GitHub issue bugfix 开发和验收期间的 source-of-truth 顺序是：`AGENTS.md` -> `docs/superpowers/plans/2026-05-16-bpmt-lite-v1.7.3-github-issue-bugfixes.md` -> `docs/release-v1.7.3.md` -> `README.md` -> implementation。
```

Update default image tag bullets from `1.7.2` to `1.7.3`.

- [ ] **Step 4: Create release notes**

Create `docs/release-v1.7.3.md`:

```markdown
# bpmt-lite v1.7.3 发布说明

## 版本定位

`v1.7.3` 是基于 `v1.7.2` 的 GitHub issue bugfix 版本，修复 OAuth 微信登录失败页提示和 OpenAPI 文档风格问题。

本版本不新增 API 路径，不改变 HMAC 认证、OAuth 主流程、HTTPS、H5 和 Docker 运行栈。

## 修复内容

- 修复 GitHub issue #18：企业微信授权成功但 BPMT 本地登录态建立失败时，错误页不再只显示“微信登录失败”，会区分平台维护/暂停、用户不存在、用户停用、IP 白名单、组织角色关系异常等可行动原因。
- 修复 GitHub issue #19：`/api/openapi.json` 的 `info.description` 和报表视图接口 summary 统一为中文描述风格，保留必要技术标识。

## 文档归档

- API 参考：[docs/v1.7.3/api-reference.md](v1.7.3/api-reference.md)
- OpenAPI 快照：[docs/v1.7.3/openapi.json](v1.7.3/openapi.json)
- 执行计划：[docs/superpowers/plans/2026-05-16-bpmt-lite-v1.7.3-github-issue-bugfixes.md](superpowers/plans/2026-05-16-bpmt-lite-v1.7.3-github-issue-bugfixes.md)

## 验证清单

- [ ] `python3 -m json.tool docs/v1.7.3/openapi.json >/tmp/bpmt-v173-openapi.json`
- [ ] `cmp -s api/src/main/webapp/openapi.json docs/v1.7.3/openapi.json`
- [ ] `git diff --check`
- [ ] `mvn -s settings.local.xml -pl platform -Dtest=OAuthWechatLoginServiceTest test`
- [ ] `mvn -s settings.local.xml -pl platform -Dtest=OAuthActionTest test`
- [ ] `mvn -s settings.local.xml -pl api -Dtest=ApiDocsContractTest test`
- [ ] `mvn -s settings.local.xml -DskipTests compile`
- [ ] `scripts/verify-repo.sh`
- [ ] `scripts/build-multiarch-images.sh`
- [ ] `docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite:1.7.3`
- [ ] `docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite-api:1.7.3`

## 发布边界

- 不升级 Java、Tomcat、MariaDB 或 nginx。
- 不改变 compose 中第三方容器版本策略。
- 不新增 API endpoint。
- 不把安装或升级状态写入业务数据库。
```

---

### Task 6: Full verification and release closure

**Files:**
- Verify: full repository
- Publish: Git tag, GHCR images, GitHub Release

- [ ] **Step 1: Run focused tests**

Run:

```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-1.8.jdk/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"
mvn -s settings.local.xml -pl platform -Dtest=OAuthWechatLoginServiceTest test
mvn -s settings.local.xml -pl platform -Dtest=OAuthActionTest test
mvn -s settings.local.xml -pl api -Dtest=ApiDocsContractTest test
```

Expected:

```text
BUILD SUCCESS
```

for all three commands.

- [ ] **Step 2: Run repo-level checks**

Run:

```bash
python3 -m json.tool docs/v1.7.3/openapi.json >/tmp/bpmt-v173-openapi.json
cmp -s api/src/main/webapp/openapi.json docs/v1.7.3/openapi.json
git diff --check
mvn -s settings.local.xml -DskipTests compile
scripts/verify-repo.sh
```

Expected:

```text
BUILD SUCCESS
```

for Maven compile, no output from `git diff --check`, and `scripts/verify-repo.sh` exits `0`.

- [ ] **Step 3: Build and publish multi-arch images**

Run:

```bash
scripts/build-multiarch-images.sh
docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite:1.7.3
docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite-api:1.7.3
docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite:latest
docker buildx imagetools inspect ghcr.io/wodenwang/bpmt-lite-api:latest
```

Expected: both `1.7.3` images and both `latest` images include:

```text
linux/amd64
linux/arm64
```

- [ ] **Step 4: Commit, tag, and push**

Run:

```bash
git status --short
git add AGENTS.md README.md pom.xml platform api docs
git commit -m "fix: release bpmt-lite v1.7.3 issue fixes"
git tag v1.7.3
git push origin HEAD:main
git push origin v1.7.3
```

Expected:

```text
[codex/bpmt-lite-v1.7.3-github-issues ...] fix: release bpmt-lite v1.7.3 issue fixes
```

and push succeeds.

- [ ] **Step 5: Create GitHub Release**

Run:

```bash
gh release create v1.7.3 \
  --repo wodenwang/bpmt-lite \
  --title "bpmt-lite v1.7.3" \
  --notes-file docs/release-v1.7.3.md
```

Expected:

```text
https://github.com/wodenwang/bpmt-lite/releases/tag/v1.7.3
```

- [ ] **Step 6: Close GitHub issues**

Run:

```bash
gh issue close 18 --repo wodenwang/bpmt-lite --comment "已在 v1.7.3 修复：OAuth 微信登录失败页会区分 BPMT 本地登录态建立失败原因，并保留 Request ID 供排查。"
gh issue close 19 --repo wodenwang/bpmt-lite --comment "已在 v1.7.3 修复：/api/openapi.json 和 /api/docs/ 的版本说明、info.description 与报表视图接口 summary 已统一为中文风格。"
```

Expected: issues `#18` and `#19` are closed.

---

## Self-Review

### Spec coverage

- `#18` 覆盖：Task 2 负责分类和日志，Task 3 负责页面标题和可行动提示，Task 6 负责发布后关闭 issue。
- `#19` 覆盖：Task 4 负责 OpenAPI 与文档页风格统一，Task 5 负责版本归档，Task 6 负责发布后关闭 issue。
- `v1.7.3` 补丁边界覆盖：Task 1 明确只处理当前 open issues，Task 5/6 只做版本、文档和发布收口。

### Placeholder scan

本计划未发现占位类表述或无具体文件/命令的空泛步骤。

### Type consistency

- 新增异常类为 `OAuthWechatLoginException`，分类器返回同一类型。
- `OAuthWechatLoginService` 继续返回现有 `OAuthWechatLoginResult.error(reason, message)`，不改变调用方对象结构。
- `OAuthAction` 只根据 `OAuthWechatLoginResult.getReason()` 选择错误页标题，不改变 OAuth redirect/code/token 协议。
- OpenAPI 只改文档文本、版本和归档快照，不改接口 path/method/schema 结构。
