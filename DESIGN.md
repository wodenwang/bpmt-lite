# bpmt-lite 桌面 UI 设计规范

> 状态：系统级长期规范。后续桌面后台 UI/UX 改造、样式修复、Product Design 审视、实现计划和 QA 都应先读本文。

## 目标

`bpmt-lite` 是遗留 BPMT 平台的简化发行工程，桌面端 UI 改造的目标不是重做一个新前端，而是在保留现有 JSP、jQuery UI、zTree、chosen、低代码表格/表单和业务提交逻辑的前提下，让系统达到基本符合主流企业后台审美、可长期维护、可逐步扩展的清晰度基线。

本规范追求三件事：

- 让用户知道自己在哪、当前页面做什么、当前状态是什么、下一步能做什么。
- 让旧控件在视觉上统一、可扫描、可点击、可理解。
- 用低成本 CSS 覆盖和少量非业务 JSP 属性补充，换取明显体验改善。

## 设计 Brief

- 产品：`bpmt-lite` 桌面后台。
- 目标用户：系统管理员、业务管理员、外部集成开发者。
- 技术现实：Java 8、JSP、jQuery 1.x、jQuery UI、zTree、chosen、旧低代码控件体系。
- 视觉目标：克制、稳定、清楚、高密度但不拥挤的企业后台；优先做到“旧系统清楚可用”，而不是“像全新产品”。
- 交互目标：主操作明确，状态反馈明确，列表/表单/弹窗可预测。
- 实施策略：保留架构和组件，先建立样式规范和状态语义，再按代表页面扩展。

## 当前选定低成本方案

当前 UI 方向收敛为：

```text
Configuration Workbench Lite
```

中文名称：低成本配置工作台。

该方案以此前选择的“Configuration Workbench + Integration Handoff”为方向，但必须向现有代码结构妥协。设计图只作为信息层级、密度和视觉气质参考，不作为像素级实现目标。

### 方案定位

- 全系统采用配置后台基线：紧凑、稳定、表格/表单友好。
- 第三方系统、OAuth、AI 接入提示词采用更清楚的接入任务表达。
- 登录页、首页、列表页、弹窗优先改善；复杂低代码业务页逐步继承通用样式。
- 不追求一次性重做壳层，不追求完整现代侧边栏，不追求替换旧图标体系。

### 必须接受的设计妥协

#### zTree 只能降噪，不能重做

zTree 当前使用老版本 sprite 图标、行内节点结构、16px 展开/文件图标、连线背景和插件生成 DOM。低成本阶段不得重写 zTree DOM 或替换整套图标系统。

允许：

- 调整字体、颜色、hover、focus、选中态。
- 适度增加节点点击区域，但不得破坏树缩进和展开命中区。
- 弱化连线、旧文件夹图标和非当前节点的视觉权重。
- 强化当前节点、当前路径和左侧面板标题。

不做：

- 不把 zTree 改成 Ant Design Menu。
- 不重绘所有 folder/file/switch sprite。
- 不改变树节点层级、展开逻辑、懒加载逻辑或右键/双击行为。
- 不为树节点加入复杂 badge、二级描述或新操作按钮。

#### jQuery UI 控件保留旧交互

jQuery UI button、tabs、dialog、datepicker 继续使用现有初始化和生命周期。

允许：

- 调整按钮、tab、dialog 的颜色、边框、圆角、focus、disabled。
- 通过现有按钮文本或少量 `title` / `aria-label` 提升可识别性。
- 弱化次要按钮，突出明确主按钮。

不做：

- 不替换成新的 Button、Tabs、Modal 组件。
- 不改变 button 初始化参数语义。
- 不依赖复杂图标库重绘所有操作按钮。

#### 表格表单继续以旧 `table` 为基础

大量页面由 `ws-table`、旧 JSP 和低代码输出生成。低成本阶段不把表单改成 CSS Grid/Flex 组件体系。

允许：

- 调整表头、行高、label 宽度、输入框高度、边框和 hover。
- 用 CSS 让搜索区、结果区、分页区层级更清楚。
- 补空状态、无匹配状态、无权限状态。

不做：

- 不大规模拆掉 `table` 布局。
- 不改变字段顺序、字段名、表单提交参数。
- 不为每个业务页面单独定制复杂布局。

#### 图表和首页只能先减噪

首页工作量图表由旧 ECharts 2.x 和已有数据逻辑驱动。低成本阶段不重写图表组件。

允许：

- 对无数据图表增加说明，降低空坐标轴噪声。
- 调整图表容器标题、间距和工具按钮视觉。

不做：

- 不升级 ECharts。
- 不新增复杂统计卡片或新的后端聚合数据。

## 第一阶段落地样板

第一阶段只围绕 4 类高收益页面建立样板，不全系统铺开：

1. 登录页：减少旧表格感，补标签、focus、错误/提示文案。
2. 登录后首页：弱化顶部账号区和 footer，强化当前模块、左树当前态、待办/公告/图表状态。
3. 第三方系统列表：采用配置工作台列表规范，搜索区、表格、空态、分页统一。
4. AI 接入提示词弹窗：采用接入交付弹窗规范，强调复制任务、反馈和手动兜底。

第一阶段完成后，再把同一套 CSS 和少量通用增强扩展到动态表、报表、流程、权限等页面。

## 非目标

以下事项不属于默认 UI 改造范围：

- 不升级前端技术栈。
- 不引入 React、Vue、Ant Design 运行时或新的 SPA 架构。
- 不重写动态表、报表、流程、权限、菜单、表单提交和 Ajax 生命周期。
- 不替换 jQuery UI、zTree、chosen、UEditor、ECharts 2.x 等第三方组件。
- 不把桌面端改造成营销型 SaaS 首页。
- 不追求大面积渐变、装饰插画、超大留白、过度圆角或卡片堆叠。
- 不为了视觉统一一次性改动全系统 DOM 结构。
- 不把已生成的概念 UI 图当作必须逐像素还原的目标。

可以参考 Ant Design 等主流后台产品的信息层级、控件密度和状态表达，但不能把“像 Ant Design”理解为替换组件库。

## 当前问题基线

2026-06-13 对 `http://127.0.0.1:18080/` 的 1440x900 桌面视口审视显示，系统已有部分 v1.8 清晰度修复，但仍存在系统性 UI/UX 问题：

- 登录页仍是表格表单结构，控件语义和视觉信任感偏弱。
- 登录后首页顶部账号信息、模块按钮、左侧树、公告、待办表格、分页、工作量图表同时争夺注意力。
- 页面整体层级偏平，标题、说明、主操作、状态、辅助信息没有稳定视觉秩序。
- zTree 旧图标、jQuery UI 按钮、表格边框、tab、分页、panel 的样式语言不统一。
- 搜索区仍像录入表格，用户需要理解很多边框后才能找到查询目标。
- 空状态已经补了文案，但常被表格框架和分页压住，缺少主操作引导。
- icon-only 操作、分页按钮和工具按钮仍依赖旧图标，识别成本高。
- footer、浮层、长表格、图表在不同内容高度下仍有遮挡或视觉干扰风险。
- 旧的 CSS-only “变白、变浅、变圆角”不能单独解决产品语义问题。

本次审视证据保存在：

```text
docs/v2.0.0/design-audit-2026-06-13/01-login-current.png
docs/v2.0.0/design-audit-2026-06-13/02-home-current.png
docs/v2.0.0/design-audit-2026-06-13/03-thirdpart-index-current.png
```

## 核心原则

### 1. 结构先于皮肤

颜色、圆角、阴影只服务信息层级。任何页面优先回答：

- 我在哪。
- 这个页面做什么。
- 当前数据或任务处于什么状态。
- 下一步推荐操作是什么。

如果页面没有标题、说明、状态和主操作，只换颜色不能算完成。

### 2. 保留后台密度

BPMT 是管理系统，不是消费级产品。列表、树、表单可以保持紧凑，但必须满足：

- 行高稳定。
- 字号可读。
- 点击目标不小于 28px。
- 主要信息可以横向扫描。
- 辅助信息不抢主内容。

### 3. 渐进驯化旧控件

旧控件不替换，但要被重新定义：

- zTree 用于导航定位，不用于制造视觉噪声；只能做选中态、hover、focus、间距和降噪，不能重做成全新侧边栏。
- jQuery UI button 用于操作，不再默认所有按钮同权重。
- `ws-table` 继续承载列表和表单，但应通过标题、说明、状态块和样式分区降低“表格堆叠感”。
- `ws-panel` 用于内容分组，不是装饰卡片。
- dialog 用于短任务，不把复杂大页面直接搬进浮层。

### 4. 状态必须有语义

loading、empty、error、success、disabled、readonly、no-permission 都必须有文字或可感知的状态表达，不能只靠颜色、图标或空表格。

### 5. 低风险优先

默认优先级是：

1. 最后加载的 CSS 覆盖层。
2. 通用 JS 初始化里的非业务增强，例如补 `title`、`aria-label`、focus class。
3. 少量 JSP class、title、aria、状态容器、空状态文案。
4. 页面局部 DOM 调整。

第 4 类必须单独评估，避免影响业务提交和动态视图生成。

## 视觉基线

### 色彩

使用中性浅色后台基线，避免单一蓝灰或大面积单色主题。

- 页面背景：浅灰蓝或中性灰，不能纯白到底。
- 主内容面板：白色或接近白色。
- 边框：低对比细线，避免厚重 3D 边框。
- 主色：稳定蓝色，只用于主操作、当前态和焦点。
- 成功、警告、危险：必须配合文本，不只靠颜色。

禁止：

- 大面积渐变背景。
- 装饰性光斑、球体、虚化背景。
- 大面积紫色、深蓝、棕橙、米色单一主题。
- 为了“现代感”过度卡片化。

### 字体

- 桌面默认正文建议 13px。
- 页面标题建议 18px 到 22px。
- 区块标题建议 14px 到 16px。
- 表格、表单、菜单不得低于 12px。
- 字重用于区分层级，不用负字距。

### 间距

- 页面外层留白保持克制，优先给主内容。
- 面板内边距建议 12px 到 20px。
- 表格行高建议 36px 到 40px。
- 搜索区字段垂直间距建议 8px 到 12px。
- 弹窗内容区 padding 建议 16px 到 20px。

### 圆角和阴影

- 常规控件圆角 4px。
- 面板、弹窗圆角 6px 到 8px。
- 阴影只用于弹窗、悬浮层和必要层级，不用于每个区块。

## 页面结构规范

### 后台壳层

顶部、模块、左侧树和主内容必须形成稳定层级：

- 顶部账号区只承载账号、组织、角色、退出、设置等低频入口，视觉权重降低。
- 模块导航必须显示当前模块，非当前模块弱化。
- 左侧树只表达当前位置和二级导航，不应比主内容更抢眼。
- 主内容区域必须有页面标题、说明或当前任务提示。
- footer 不能固定遮挡内容，视觉存在感应低于主内容。

### 首页

首页不是控件堆叠页，应表现为工作状态概览：

- 当前账号和工作区信息简明。
- 待办、公告、工作量等区块有清楚标题。
- 空状态说明“暂无待办”或“暂无公告”，不显示空白表格。
- 图表无数据时显示无数据说明，不保留空坐标轴作为主要内容。
- loading 必须说明正在加载哪个区块。

### 列表页

列表页必须包含：

- 页面标题。
- 页面用途说明。
- 主操作。
- 搜索区。
- 结果区。
- 分页区。
- 空/错/无权限状态。

搜索区要求：

- 与结果区视觉分离。
- label 对齐稳定。
- 输入框高度统一。
- 查询为主按钮，重置为次按钮。
- 高级筛选默认弱化。

结果表格要求：

- 表头浅灰背景，中等字重。
- 行高稳定，hover 可见。
- 状态列使用文字或轻量 badge。
- 时间、数字、ID、URL 长文本要有截断和 `title`。
- 行操作按钮固定尺寸，并有 `title` 或 `aria-label`。

分页要求：

- 左侧统计和右侧按钮垂直居中。
- 当前页、禁用态、可点击态清楚。
- 无数据时分页不应成为视觉焦点。

### 表单页

表单页必须让用户看清字段关系：

- label 宽度稳定。
- 必填项统一标识。
- readonly 与 disabled 明确区分。
- 错误提示靠近字段。
- 保存、提交、取消、重置位置统一。
- 长文本输入使用合理高度和滚动，不撑破页面。

### 弹窗

弹窗只用于短任务：

- 标题说明任务。
- 副说明解释使用场景。
- 内容区不嵌套过多表格边框。
- 底部操作区固定层级，主操作和关闭/取消分离。
- 长内容可滚动，不超过视口。
- 关闭按钮必须可见且有可访问名称。

AI 接入提示词这类弹窗应按“复制任务”设计：

- 文案说明复制给谁、用于什么。
- 提示词区域只读。
- 主操作为复制。
- 成功和失败都有反馈。
- 失败时提供手动复制路径。

## 状态规范

### Loading

- 写明正在加载的对象。
- 避免只显示条纹 loading。
- 长时间加载需要失败或重试路径。

### Empty

至少区分：

- 无数据：说明还没有配置，并提供新增入口。
- 无匹配：说明筛选无结果，并提供重置查询。
- 无权限：说明当前账号无法查看，并提示联系管理员或调整授权。

### Error

- 说明失败对象。
- 给出可尝试动作。
- 不暴露敏感堆栈。
- 必要时显示 requestId 或日志定位线索。

### Success

- 保存、复制、删除、启用、停用等操作完成后必须反馈。
- 反馈不能只靠颜色。

### Focus

- 键盘 Tab 时必须可见。
- 覆盖链接、按钮、输入框、树节点、tab、弹窗关闭按钮。

### Disabled 和 Readonly

- disabled 必须看起来不可点击，并使用 `not-allowed` cursor。
- readonly 应保留可读性，不能像 disabled 一样弱到看不清。

## 允许的改动

默认允许：

- 调整 `bpmt-modern.css` 或后续同类最后加载 CSS 覆盖层。
- 调整颜色、字号、间距、边框、圆角、hover、focus、disabled。
- 统一 jQuery UI、zTree、chosen、ws-table、ws-bar、dialog 的视觉。
- 为 icon-only 按钮补 `title`、`aria-label`。
- 为页面和区块补 class、role、aria、状态文案。
- 为无数据、无匹配、无权限、加载失败补轻量状态容器。
- 为复制、保存、删除等操作补非侵入式反馈。

需要评估后才允许：

- 调整壳层尺寸。
- 调整左侧树节点高度。
- 调整分页 DOM。
- 调整表单表格布局。
- 调整弹窗按钮区结构。

默认禁止：

- 改业务 Action。
- 改数据库结构。
- 改权限模型。
- 改动态视图配置协议。
- 改 API 鉴权。
- 改表单字段命名和提交参数。
- 改 Ajax 生命周期。
- 重写第三方组件初始化逻辑。

## 低成本落地顺序

### P0：统一覆盖层和回滚点

- 保持 `bpmt-modern.css` 最后加载，作为默认样式覆盖层。
- 明确删除或注释该 CSS 引入即可回滚旧主题。
- 先统一字体、颜色、输入框、按钮、表格、分页、dialog、focus、disabled。
- 不进入业务 JSP 逐页重构。

### P1：壳层减噪

- 顶部账号区：只做字号、颜色、间距、弱化，不改信息来源。
- 模块导航：强化当前模块，统一按钮尺寸和选中态。
- 左侧 zTree：只做降噪、hover、focus、当前节点，不替换图标系统。
- footer：降低视觉权重，检查是否遮挡内容。

### P2：通用列表和搜索区

- 搜索区：在旧 `table` 上通过背景、边框、label 宽度和按钮层级实现视觉分区。
- 表格：统一表头、行高、hover、长文本截断、状态文本/badge。
- 分页：尽量 CSS flex 覆盖旧 float/absolute 布局；必要时小改 `page_bar.jsp`。
- 空状态：先做通用兜底，再对第三方系统等重点页面补更明确动作。

### P3：接入类页面样板

- 第三方系统列表按“配置工作台”样式做样板。
- AI 接入提示词弹窗按“接入交付”样式做样板。
- OAuth/微信/AI 相关状态可以使用轻量状态标签，但不新增复杂流程。
- 复制成功/失败反馈可以通过现有 `Ui.msg` 或局部提示实现。

### P4：扩展到高频旧页面

每轮只选择 3 到 5 个代表页面扩展，不全系统铺开。优先顺序：

- 动态表开发列表。
- 报表列表。
- 流程设置列表。
- 用户/权限相关列表。
- 邮件、微信、任务、队列等管理列表。

扩展时只复用已经验证的样式和状态模式，不为每个页面重新发明布局。

## 设计图到实现的取舍规则

后续概念图或 UI 图必须经过以下取舍后才能进入实现：

- 图中现代侧边栏效果，落地时转换为 zTree 降噪和当前态增强。
- 图中高级状态芯片，落地时优先用文本 + 轻量 badge，不新增复杂组件。
- 图中整洁筛选工具栏，落地时优先在旧查询表格上做视觉分区。
- 图中弹窗底部操作区，落地时优先用现有 dialog button 或少量 JSP 底部按钮。
- 图中图标按钮，落地时优先保留 jQuery UI icon，并补 `title` / `aria-label`。
- 图中大面积信息卡片，落地时转换为现有 panel 内的标题、说明和状态块。

如果某个视觉目标需要重写 DOM、替换控件或改变业务生命周期，默认降级为“可读性改善”，不作为本轮必须完成项。

## 验收门禁

每次 UI 改造完成前必须留下证据：

- 1440x900 桌面截图。
- 至少一个窄桌面或较小内容区截图。
- 关键路径操作记录。
- console error 检查。
- focus 可见性检查。
- 空、错、加载、无权限至少覆盖相关页面中的一种。
- 回滚方式说明。

完成判定不能写“应该没问题”。必须基于当次截图、浏览器检查、命令输出或人工可复核记录。

## 实施文件边界

优先文件：

```text
platform/src/main/webapp/css/bpmt-modern.css
platform/src/main/webapp/css/jquery-ui-ext.css
platform/src/main/webapp/css/styles.css
platform/src/main/webapp/css/styles_light.css
platform/src/main/webapp/css/frame.css
```

必要时少量补充：

```text
platform/src/main/webapp/include/html_include.jsp
platform/src/main/webapp/xhtml/common/page_bar.jsp
platform/src/main/webapp/xhtml/frame/*.jsp
platform/src/main/webapp/xhtml/frame_new/*.jsp
platform/src/main/webapp/xhtml/dyn/*.jsp
platform/src/main/webapp/xhtml/report/*.jsp
platform/src/main/webapp/xhtml/flow/**/*.jsp
platform/src/main/webapp/xhtml/thirdpart/ThirdpartAction/*.jsp
platform/src/main/webapp/js/jquery-ui-ext.js
platform/src/main/webapp/js/ws-core-ext.js
platform/src/main/webapp/js/ws-ui.js
```

只有当 CSS 无法表达必要状态时，才修改 JSP 或 JS。修改 JSP/JS 时必须说明“不影响业务逻辑”的理由。

## 后续 Agent 执行要求

后续 agent 在做 UI 改造前必须：

1. 先读 `AGENTS.md` 和本文。
2. 明确本轮只处理哪些页面和状态。
3. 明确是否只改 CSS，是否允许补 JSP 属性。
4. 先保留当前态截图。
5. 再做实现计划。
6. 完成后用截图和浏览器检查证明结果。

如果改动不能在不影响业务逻辑的前提下完成，应停止并重新设计更小的 vertical slice。
